# Risks

Track material risks, impact, mitigation, and current status.

| Risk | Impact | Mitigation | Status |
|---|---|---|---|
| Compliance uncertainty for AI calls and recordings in Spain/EU | Product may be legally unsafe to operate commercially | Legal review before production/commercial use; conservative AI disclosure; avoid recording until policy is clear | Open |
| Over-disclosure of personal data during calls | Privacy/security harm | Policy engine, per-task allowed facts, approvals for sensitive facts, audit log | Open |
| Voice worker reliability during long calls | Failed calls and poor UX | Long-lived worker outside serverless; retries; recovery state in DB; health checks | Open |
| Latency in STT/LLM/TTS loop | Unnatural conversations | Choose low-latency providers; measure end-to-end latency; keep prompts/context small | Open |
| Cost runaway during hold music or silence | Unexpected bills | Hold/silence detection; cost tracking; pause expensive processing during waits | Open |
| Agent context bloat during development | Slow and inconsistent AI-assisted development | Hot/warm/cold docs; epic-driven context; session closeout workflow | Mitigated by docs scaffold |
| Provider pricing/model drift | Incorrect cost estimates or outdated implementation choices | Recheck provider docs before implementation and before production | Open |
| Turn detection loses early/short callee utterances | Callee repeats themselves, conversations feel broken | Tuned `VADParams` + `LocalSmartTurnAnalyzerV3` wired onto the user aggregator (PR #7; old `vad_analyzer=` kwarg was a silent no-op); InboundAudioProbe kept | Mitigated in code 2026-06-11, awaiting live validation |
| Small conversation LLM drifts out of caller role mid-call | Agent acts as the callee's receptionist; task fails, callee confused | Explicit role block + language-aware few-shot (PR #7); `doctor/role_drift_probe` 5/5; BUT still intermittent at the data/wrap-up stage when the agent lacks a datum — live: asked the callee "¿cuántas personas?" (1/3 `booking_third_party`), `booking_basic` role 2/5 — few-shot covers names, not other missing data (#2, out of D-14 scope) | Partially mitigated; data-stage drift open |
| Agent states the OWNER's name instead of the booking name | Wrong reservation; task fails; confusing to callee | Root cause: `allowed_facts` was a profile-key whitelist, dropping task-specific names. Fixed D-14: `call_facts` channel + `DETAILS FOR THIS CALL` block + few-shot rewrite; `restaurant/booking_third_party` eval ×3 states Victoria, never Nikita | Mitigated in code 2026-06-12 (branch `feature/call-data-and-termination`); awaiting live phone confirmation |
| Agent over-claims a result it did not achieve | Misleading summary; user acts on false confirmation | Live: agent said "la reserva está confirmada" when nothing was booked. Eval success axis has an over-claim guard (achieved vs expected); prompt-side anti-over-claim wording not yet added (#4); D-15: ground result-claims in tool results (only emit "confirmed" from a tool success) | Open (found 2026-06-12 on the real call; direction set D-15) |
| STT mishears the callee organisation / proper nouns | Wrong context, possible wrong actions | Live: "Pizza Parking" → "Pisopaylink" (Deepgram es). Consider phrase hints/keyterms from `target_name`/`call_facts`; measure | Open (found 2026-06-12) |
| Transcript stored word-by-word with synthetic `ts_ms` | Unreliable latency/analytics; noisy audit trail | `_CallObserver` emits per `TTSTextFrame`/`TranscriptionFrame`; aggregate to per-utterance segments with real timestamps | Open (found 2026-06-12) |
| High-sensitivity fact values present in LLM prompt | Prompt injection by callee could elicit them without approval | `[SENSITIVE]` marker + approval rule in prompt (done); structural value withholding until approval; eval policy axis now detects leaks (one unapproved DNI disclosure observed 2026-06-12) | Partially mitigated; leak now measurable offline |
| Worker never sets `fact_key` on `ActionRequest` | Policy engine's fact-access deny branch is unreachable from production calls | Pass `fact_key` from `tools.py` so per-fact access rules can fire; covered as D-13 open item | Open (found 2026-06-12 during eval-harness review) |
| haiku omits `end_call`/`propose_summary` at conversation end | Calls trail off without clean termination/summary; runs stuck "running" | D-14: prompt nudge ("you MUST call end_call") + deterministic in-call backstop (`TerminationGuard` duration/turn caps) guarantees a hangup + terminal status in production. Voluntary `end_call` still unreliable on haiku (live `--runs 5`: info 2/5, booking 3/5, role_drift 5/5); backstop covers it, model-floor reassessment open (D-11) | Mitigated for production termination 2026-06-12; voluntary rate still open |
| Dev-stand fragility: network blips kill api process and quick-tunnel registration | Lost live-session time, silent dead stand | Worker queue loop hardened (2026-06-11); api/bot resilience + process supervision + named tunnel/VPS pending (D-12) | Open (api+bot found dead again 2026-06-11) |
| Scenario routing built but not wired into intake | All tasks evaluate on the `generic` policy profile; doctor/insurance/restaurant rules and scenario-scoped facts never activate | Wired 2026-06-12 (D-13, PR #10): `normalize.py` extracts `scenario`, confirm card shows it with one-tap correction, `generic` on unsure | Mitigated 2026-06-12 |
| Conversation LLM below the reliability floor (haiku-tier) for multi-turn tool-using voice | Most live defects: role drift, result over-claim, `end_call` omission, wrong data | Model-floor swap to Gemini 2.5 Flash / GPT-4.1 (config-only, D-5); Pipecat Flows lowers per-node difficulty; external benchmarks (Daily.co) put the prod floor near GPT-4.1 / Gemini 2.5 Flash | Open — root-caused in the D-15 audit (2026-06-14); reopens D-11 |
| Monolithic single-prompt agent → context rot; fixing each symptom with another prompt rule degrades instruction-following | Recurring, compounding behaviour bugs that resist prompt fixes | Migrate dialog to Pipecat Flows: per-node scoped sub-prompt + only the tools valid in that node, so role / data / over-claim drift become structurally impossible (D-15) | Open — direction set D-15; execution via brainstorm→spec→plan |
| Eval harness (text edges) blind to audio / STT / termination-backstop | False confidence: real-call defects (STT mishear, backstop, telephony timing) ship unseen; D-11's single-turn A/B gave a false "haiku holds role" | Keep the LLM-judge text harness as the fast inner loop; add an audio-in-the-loop tier (TTS→STT→agent) (D-15) | Open — direction set D-15 |

